{
  "config": {
    "step": {
      "user": {
        "title": "Solar Catch setup",
        "description": "Select the excess-power sensor and the appliance switch, then set the basic control values.",
        "data": {
          "power_entity": "Excess power sensor",
          "appliance_switch": "Appliance switch",
          "appliance_power_entity": "Appliance power sensor (optional)",
          "appliance_power_w": "Fallback appliance draw",
          "start_threshold_w": "Start power threshold",
          "above_threshold_seconds": "Above threshold time",
          "below_threshold_seconds": "Below threshold time",
          "min_runtime_minutes": "Minimum daily runtime",
          "start_time": "Start time",
          "end_time": "End time",
          "enabled": "Enabled",
          "top_up_enabled": "Top Up",
          "control_mode": "Mode"
        },
        "data_description": {
          "power_entity": "Must read positive when exporting/excess power is available, and negative when importing. Create an inverted template sensor if your meter is the opposite sign.",
          "appliance_power_entity": "Optional live power draw sensor for the controlled appliance. If supplied, Solar Catch uses this instead of the fixed fallback draw while the appliance is on.",
          "appliance_power_w": "Used only when no live appliance power sensor is selected, or if that sensor is unavailable.",
          "top_up_enabled": "When off, Solar Catch only runs on excess solar. When on, it also forces a late top-up so the minimum runtime is reached by the end time.",
          "control_mode": "Auto follows Solar Catch logic. Force On turns the appliance on regardless. Force Off turns the appliance off regardless."
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Solar Catch options",
        "description": "Update the Solar Catch controller settings.",
        "data": {
          "power_entity": "Excess power sensor",
          "appliance_switch": "Appliance switch",
          "appliance_power_entity": "Appliance power sensor (optional)",
          "appliance_power_w": "Fallback appliance draw",
          "start_threshold_w": "Start power threshold",
          "above_threshold_seconds": "Above threshold time",
          "below_threshold_seconds": "Below threshold time",
          "min_runtime_minutes": "Minimum daily runtime",
          "start_time": "Start time",
          "end_time": "End time",
          "enabled": "Enabled",
          "top_up_enabled": "Top Up",
          "control_mode": "Mode"
        },
        "data_description": {
          "power_entity": "Must read positive when exporting/excess power is available, and negative when importing. Create an inverted template sensor if your meter is the opposite sign.",
          "appliance_power_entity": "Optional live power draw sensor for the controlled appliance. If supplied, Solar Catch uses this instead of the fixed fallback draw while the appliance is on.",
          "appliance_power_w": "Used only when no live appliance power sensor is selected, or if that sensor is unavailable.",
          "top_up_enabled": "When off, Solar Catch only runs on excess solar. When on, it also forces a late top-up so the minimum runtime is reached by the end time.",
          "control_mode": "Auto follows Solar Catch logic. Force On turns the appliance on regardless. Force Off turns the appliance off regardless."
        }
      }
    }
  }
}
